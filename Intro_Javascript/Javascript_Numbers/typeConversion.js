// 5️⃣ Type Conversion (Number ↔ String)

let strNum = "100";
let converted = Number(strNum); // string → number
console.log(converted + 50);    // 150

let num = 123;
let str = String(num);           // number → string
console.log(str + " USD");       // "123 USD"
