# Different Types of Function Parameters in JavaScript

এই folder এ JavaScript function এর বিভিন্ন ধরনের parameter শেখা হয়েছে।

🧠 What is a Parameter? (Quick Recap)

👉 Parameter হলো function এর ভেতরে ব্যবহৃত input variable
👉 function call করার সময় যেগুলো পাঠানো হয়, সেগুলো argument

## Types Covered

1. Normal parameter
2. Multiple parameters
3. Default parameter
4. Rest parameter (...)
5. Object parameter
6. Destructured object parameter
7. Array parameter
8. Destructured array parameter

## Why Parameters Are Important

- Function কে dynamic করে
- Reusable logic তৈরি হয়
- Clean & readable code লেখা যায়

## Key Notes

- Default parameter ব্যবহার করলে missing argument handle করা যায়
- Rest parameter দিয়ে unlimited argument নেওয়া যায়
- Destructuring parameter code clean করে
