# (Advanced) If–Else Shorthand: Ternary Operator

এই অধ্যায়ে আমরা শিখবো  
**if–else এর shorthand version**, যাকে বলা হয় **Ternary Operator**।

এটা মূলত clean ও short code লেখার জন্য ব্যবহার করা হয়।

---

## 📌 Ternary Operator কী?

Ternary operator হলো—
👉 if–else এর shorthand (short form)

এতে তিনটা অংশ থাকে:

1. condition
2. true হলে কী হবে
3. false হলে কী হবে

---

## 🔹 Syntax

```js
condition ? trueValue : falseValue



------
🧠 What I Learned (Summary)

Ternary operator হলো if–else এর short form

Single condition এর জন্য ternary best

Code clean ও short হয়

Complex logic এ if–else ভালো

Nested ternary limited ভাবে ব্যবহার করা উচিত

❓ Question & Answer (Self Check)
Q1: Ternary operator কেন ব্যবহার করা হয়?

Answer:
if–else এর short ও clean version লেখার জন্য।

Q2: Ternary operator এ কয়টা অংশ থাকে?

Answer:
৩টা অংশ — condition, true value, false value।

Q3: Nested ternary কখন ব্যবহার করা উচিত?

Answer:
যখন condition কম এবং logic simple থাকে।

Q4: Complex logic এ ternary ব্যবহার করা ঠিক?

Answer:
না, complex logic এ if–else বেশি readable।

Q5: Ternary কি if–else replace করে?

Answer:
না, এটা if–else এর alternative, replace না।
```
