# Conditional Branching: if – else (JavaScript)

এই অধ্যায়ে আমরা শিখবো JavaScript এর  
**Conditional Branching** বা **if – else condition**।

Condition অনুযায়ী program কীভাবে আলাদা আলাদা সিদ্ধান্ত নেয়,
সেটাই এখানে বিস্তারিতভাবে ব্যাখ্যা করা হয়েছে।

---

## 📌 What is Conditional Branching?

Conditional Branching মানে হলো—

- যদি condition `true` হয় → এক পথ
- যদি condition `false` হয় → অন্য পথ

অর্থাৎ program condition অনুযায়ী **ভিন্ন ভিন্ন পথে (branch)** চলে।

---

## 🔹 Condition কী?

Condition হলো এমন একটা expression  
যার result সবসময় হয়:

- `true`
- `false`

Example:

```js
10 > 5; // true
5 === 10; // false
```
