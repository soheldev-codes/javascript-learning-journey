# JavaScript Run Basics 🚀

এই folder এ আমি JavaScript কীভাবে run হয় সেটা শিখছি।

## JavaScript কোথায় run হয়?

JavaScript প্রধানত দুই জায়গায় run হয়:

1. Browser
2. Node.js

---

## 1. Browser এ JavaScript Run

Browser এ JavaScript run করতে HTML file এর ভিতরে `<script>` tag ব্যবহার করা হয়।

Example:

- index.html
- script.js

Browser এর Developer Console এ output দেখা যায়।

---

## 2. Node.js দিয়ে JavaScript Run

Browser ছাড়া JavaScript run করতে Node.js ব্যবহার করা হয়।

Command:
node file_name.js
Output terminal এ দেখা যায়।

---

## What I learned ✅

- JavaScript browser এ run করতে পারে
- Node.js দিয়ে terminal এ run করা যায়
- console.log() দিয়ে output দেখা যায়

---

📌 This is part of my JavaScript learning journey.
