# (Advanced) Logical NOT Operator (`!`) in JavaScript

এই অধ্যায়ে আমরা শিখবো **Logical NOT Operator (`!`)**।  
এটা boolean value কে **উল্টে দেয়**।

---

## 🎯 What You Will Learn

- Logical NOT (`!`) কী এবং কিভাবে কাজ করে
- Boolean value কে উল্টানো
- `if` statement এর সাথে ব্যবহার
- AND / OR logical operator এর সাথে ব্যবহার
- Ternary operator এর সাথে ব্যবহার

---

## 🔹 Logical NOT Operator কী?

- `!` operator value কে উল্টে দেয়
- true → false
- false → true

### Syntax

```js
!condition


-----

🧠 What I Learned (Summary)

! operator boolean উল্টায়

condition এর logical decision easy করে

AND / OR এর সাথে combination powerful

Ternary এর সাথে use করলে short & clean code হয়

❓ Question & Answer (Self Check)
Q1: ! operator কি করে?

Answer: boolean value উল্টে দেয়

Q2: !true কি হবে?

Answer: false

Q3: !false কি হবে?

Answer: true

Q4: AND / OR এর সাথে NOT কেন ব্যবহার করা হয়?

Answer: condition এর opposite চেক করার জন্য

Q5: Ternary operator এর সাথে NOT ব্যবহার করা যায় কি?

Answer: হ্যাঁ, true/false উল্টে clean short code লেখা যায়

```
