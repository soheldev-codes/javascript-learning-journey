# Multiple Conditions & Logical Operators (JavaScript)

এই অধ্যায়ে আমরা শিখবো কীভাবে  
একাধিক condition একসাথে ব্যবহার করে decision নেওয়া যায়।

এখানে মূলত দুইটি logical operator ব্যবহার করা হয়:

- AND (&&)
- OR (||)

---

## 📌 Logical Operator কী?

Logical Operator ব্যবহার করা হয়—

- একাধিক condition combine করার জন্য
- complex decision নেওয়ার জন্য

সব logical expression এর result হয়:

- true
- false

---

## 🔹 AND Operator (&&)

সব condition `true` হলে result `true` হবে।  
একটাও `false` হলে result `false`।

---

🧠 What I Learned (Summary)

Logical operator দিয়ে একাধিক condition handle করা যায়

&& ব্যবহার হয় strict decision এর জন্য

|| ব্যবহার হয় flexible decision এর জন্য

if–else এর সাথে logical operator খুব powerful

Real-life logic build করতে এটা খুব গুরুত্বপূর্ণ

❓ Question & Answer (Self Check)
Q1: Logical operator কেন ব্যবহার করা হয়?

Answer:
একাধিক condition একসাথে check করে decision নেওয়ার জন্য।

Q2: && operator কখন true হয়?

Answer:
সব condition true হলে।

Q3: || operator কখন true হয়?

Answer:
যেকোনো একটি condition true হলেই।

Q4: Login system এ কোন operator বেশি ব্যবহার হয়?

Answer:
সাধারণত && operator (username && password check করার জন্য)।

Q5: Role based access এ কোন operator ভালো?

Answer:
|| operator (admin অথবা moderator হলে access দেওয়ার জন্য)।
